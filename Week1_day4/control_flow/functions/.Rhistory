char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence")
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence")
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
library(tidyverse)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence")
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
#absolute values - returns a positive number
abs(-2.2)
#round
round(5.1234567890) #by default rounds to 0 dp
round(5.1234567890, digits = 2)
#always rounds down
floor(5.9)
#round up
ceiling(6.1)
#concatenation
paste("Hello", "Bye")
paste0("Hello", "Bye")
paste(letters, collapse = " ")
sort(c("c", "f", "a"), decreasing = TRUE)
#number of chsrs
nchar("hello")
nchar(c("hello", "bye"))
by_five <- function(x) {
return(x * 5)
}
by_five(10)
#multiple inputs
mult <- function(x, y=5) {
return(x * y)
}
mult(2, 3)
mult(2)
make_price <- function(x) {
paste0("£", x)
}
make_price(123)
animals <- c("Cat", "Dog", "Monkey", "Elephant", "Frog")
hats <- c("Bowler", "Sun Hat", "Baseball Cap", "Fez")
trees <- c("Ash", "Elm", "Oak", "Willow", "Fir")
animals_sorted <- sort(animals)
animals_joined <- paste(animals_sorted, collapse = " ")
paste("Ordered list:", animals_joined)
make_ordered_list <- function(my_list) {
my_list_sorted <- sort(my_list)
my_list_joined <- paste(my_list_sorted, collapse = ", ")
paste("Ordered list:", my_list_joined)
}
make_ordered_list(animals)
make_ordered_list(hats)
make_ordered_list(trees)
debugonce((make_ordered_list))
make_ordered_list(hats)
library(tidyverse)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence")
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence")
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds")
)
}
make_fancy_price(8)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
price[[1]][2]
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == NULL ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == NULL ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == NULL ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
price[[1]][2]
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == NULL ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == NULL ~ paste(price[[1]][2], "pence"),
price[[1]][2] == NULL ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(25)
price[[1]][2]
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
price[[1]][2]
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][1], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][1], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price[[1]][1]) ~ paste(price[[1]][1], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
price[[1]][2]
price[[1]][1]
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1]) == "0" ~ paste(price[[1]][2], "pence"),
price[[1]][2]
price[[1]][2]
price[[1]][1]) == "0"
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price(0.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price(3.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price(123.25)
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
?tryCatch
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
tryCatch(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
finally = "What's the price?")
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
tryCatch(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
finally = "What's the price?")
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
tryCatch(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence"),
finally = "What's the price?")
)
}
make_fancy_price()
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price) ~ print("What's the price?")
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(price) ~ print("What's the price?"),
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x = "Please enter price"){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x = "Please enter price in"){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x = "Please enter price"){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(x) ~ paste("Please enter price"),
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(char) ~ paste("Please enter price"),
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x = ""){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is.na(char) ~ paste("Please enter price"),
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
price
make_fancy_price <- function(x = ""){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
is_empty(price) ~ paste("Please enter price"),
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price()
make_fancy_price <- function(x){
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
make_fancy_price(1.25)
make_fancy_price <- function(x){
if (x == NULL) {
paste("Please enter price")
} else {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
}
make_fancy_price(1.25)
make_fancy_price <- function(x){
if (x == NULL) {
paste("Please enter price")
} else {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
}
make_fancy_price()
make_fancy_price <- function(x = NULL){
if (x == NULL) {
paste("Please enter price")
} else {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
}
}
make_fancy_price()
make_fancy_price <- function(x = NULL){
if (!is.null(x)) {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
} else {
paste("Please enter price")
}
make_fancy_price()
make_fancy_price <- function(x = NULL){
if (!is.null(x)) {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
} else {
paste("Please enter price")
}
}
make_fancy_price()
make_fancy_price <- function(x = NULL){
if (!is.null(x)) {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
} else {
paste("Please enter price")
}
}
make_fancy_price(3.25)
make_fancy_price <- function(x = NULL){
if (!is.null(x)) {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
} else {
paste("Please enter price")
}
}
make_fancy_price(0.25)
make_fancy_price <- function(x = NULL){
if (!is.null(x)) {
char <- as.character(x)
price <<- stringi::stri_split_fixed(char,".")
case_when(
price[[1]][1] == "0" ~ paste(price[[1]][2], "pence"),
is.na(price[[1]][2]) ~ paste(price[[1]][1], "pounds"),
TRUE ~ paste(price[[1]][1], "pounds", price[[1]][2], "pence")
)
} else {
paste("Please enter price")
}
}
make_fancy_price(123.25)
